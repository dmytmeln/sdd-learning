" .ideavimrc is a configuration file for IdeaVim plugin. It uses
"   the same commands as the original .vimrc configuration.
" You can find a list of commands here: https://jb.gg/h38q75
" Find more examples here: https://jb.gg/share-ideavimrc

let mapleader = " "
nmap <space> <Leader>

"" -- Suggested options --
" Show a few lines of context around the cursor. Note that this makes the
" text scroll if you mouse-click near the start or end of the window.
set scrolloff=5
set clipboard+=unnamed
set relativenumber number
" Do incremental searching.
set incsearch

" Don't use Ex mode, use Q for formatting.
map Q gq

" --- Enable IdeaVim plugins https://jb.gg/ideavim-plugins

" Highlight copied text
Plug 'machakann/vim-highlightedyank'
" Commentary plugin
Plug 'tpope/vim-commentary'
Plug 'preservim/nerdtree'

"" -- Map IDE actions to IdeaVim -- https://jb.gg/abva4t
"" Map \r to the Reformat Code action
"map \r <Action>(ReformatCode)

"" Map <leader>d to start debug
"map <leader>d <Action>(Debug)

"" Map \b to toggle the breakpoint on the current line
"map \b <Action>(ToggleLineBreakpoint)

map <leader>crn <Action>(RunClass)
map <leader>run <Action>(Run)


map <leader>sim <Action>(IntentionActionAsAction_com.intellij.codeInsight.intention.impl.AddSingleMemberStaticImportAction)

map <leader>kl <Action>(CommentByLineComment)
map <leader>k <Action>(QuickJavaDoc)

" Refactoring
map <leader>v <Action>(IntroduceVariable)
map <leader>em <Action>(ExtractMethod)
map <leader>rn <Action>(RenameElement)
map <leader>oi <Action>(OptimizeImports)
map <leader>oc <Action>(ReformatCode)
map <leader>oa <Action>(ReformatCode)<Action>(OptimizeImports)
map <leader>in <Action>(Inline)

" Navigation
map <leader>b <Action>(Back)
map <leader>f <Action>(Forward)
map <A-l> <Action>(NextSplitter)
map <A-n> <Action>(NextTab)
map <A-p> <Action>(PreviousTab)
map <A-k> <Action>(MethodUp)
map <A-j> <Action>(MethodDown)
map <leader>ff <Action>(GotoFile)
map <leader>fc <Action>(GotoClass)
map <leader>fa <Action>(GotoAction)
map gi <Action>(GotoImplementation)
map <leader>go <Action>(GotoSuperMethod)
map gt <Action>(GotoTypeDeclaration)

map <leader>sf <Action>(NewScratchFile)

map <A-s><A-r> <Action>(SplitVertically)
map <A-s><A-d> <Action>(SplitHorizontally)
map <A-m><A-r> <Action>(MoveEditorToOppositeTabGroup)

map <leader>cc <Action>(CloseContent)

map <leader>a <Action>(ShowIntentionActions)

map <A-C-k> <Action>(MoveStatementUp)
map <A-C-j> <Action>(MoveStatementDown)

map <leader>eti <Action>(ExtractInterface)

map <C-l><C-l><C-m> <Action>(AIAssistant.Editor.AskAiAssistantInEditor)

imap <C-s> <Action>(ParameterInfo)
map <C-s> <Action>(ParameterInfo)

vmap <C-e><C-m> <Action>(ExtractMethod)
vmap <C-k><C-b> <Action>(CommentByBlockComment)